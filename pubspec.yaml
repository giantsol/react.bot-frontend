name: fb_app
description: App for Facebook hackerthon

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  mic_stream: ^0.2.0+2
  audio_streams: ^1.0.0+2
  camera: ^0.5.7+4

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/
